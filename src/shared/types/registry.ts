export interface RegistryCourse {
  id: string;
  title: string;
  description: string;
  tags: string[];
  objective: string;
  courseRepo: string;
  sourceRepo: string;
  authorGitHub: string;
  latestVersion: string;
  addedAt: string;
}
